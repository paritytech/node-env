---
description: Start the Paseo dev stack (paseo node + eth-rpc) in tmux
allowed-tools: Bash(deno run:*), Bash(tmux:*)
argument-hint: [--build]
---

Start the Paseo (Passet Hub) development stack in tmux.

User provided these flags: `$ARGUMENTS`

```
deno run --allow-all ~/github/node-env/mod.ts paseo-dev-stack $ARGUMENTS
```
